# 家书星球当前进展快照

> **历史快照：已被替代。** 本文记录 2026-07-04 当时的工程状态，不是当前操作或规划依据。当前状态统一见 [`当前项目状态.md`](./当前项目状态.md)。

**日期**：2026-07-04

**用途**：定格当前主线代码、已完成阶段、可验证能力、外部阻塞项与下一步动作，避免后续继续推进时重复盘点。

## 1. 当前分支与状态

- 工作分支：`feature/launch-foundation-phase1-auth-persistence`
- 当前 HEAD：`c8c4064464fd43bbd9b3bec0355fc76a62ed8251`
- 当前 worktree：干净

## 2. 本阶段已完成的任务

### Task 1：运行时配置与依赖地基

已完成：

- Auth / Prisma / Resend 依赖接入
- `src/server/config/env.ts`
- 环境变量测试

### Task 2：Prisma Schema 与个人星系引导

已完成：

- `prisma/schema.prisma`
- `src/server/db/client.ts`
- `src/server/db/galaxy-repo.ts`
- `src/server/services/galaxy.service.ts`

### Task 3：登录与受保护路由

已完成：

- Auth.js 登录链路
- `/sign-in`
- `middleware.ts`
- 根路由重定向
- Edge-safe auth split
- Prisma 7 lazy singleton 兼容

### Task 4：认证后的 `/galaxy` 主入口

已完成：

- `src/server/services/home.service.ts`
- `/galaxy` 认证取数
- `GalaxyWorkspace initialPlanets`
- 主交互对 server-provided planets 的动态锚点适配

### Task 5：分享存储切到 Prisma

已完成：

- `src/server/db/shared-book-repo.ts`
- `src/server/store/shared-books.ts` 改为 Prisma 主存储
- `/api/books/publish` 绑定登录用户
- 旧 `.local-data/shared-books.json` 只读 fallback

### Task 6：文档与验证收口

已完成：

- README 补认证基线启动说明
- 本地验证收口

## 3. 当前已验证通过的命令

已实跑通过：

```bash
npx pnpm prisma generate
npx pnpm test
npx pnpm lint
npx pnpm build
```

当前测试结果：

- `19` 个测试文件通过
- `68` 个测试通过

## 4. 当前主线真实能力

当前已经具备的真实能力：

- 用户可通过 Auth.js 登录
- 受保护页面具备访问控制
- 用户首次进入 `/galaxy` 时可触发个人星系引导
- 分享数据主存储已经切到 Prisma 模型
- 分享发布与公开访问链路存在数据库主路径

## 5. 当前仍然保留的演示性质

以下内容仍偏演示 / Mock：

- `GalaxyWorkspace` 大量 UI 场景仍是演示壳
- 记忆、共鸣、家书生成流程仍混合使用 mock 数据与 demo loop
- 图片 / 语音产品化尚未落地
- `/galaxy` 页面内部不少内容仍是演示性星图与场景文案

## 6. 当前主要可维护性风险

### 6.1 `galaxy-workspace.tsx` 仍然过大

现状：

- 页面状态、交互、演示逻辑集中在一个大文件中

影响：

- 后续继续接真实业务时，回归风险高

### 6.2 服务层刚建立，但尚未完全覆盖主线

现状：

- `auth`、`galaxy`、`home`、`shared-book` 基础边界已出现
- `memory`、`resonance`、`book` 还未全部服务化

### 6.3 本地数据库环境未接通

现状：

- 当前 worktree 无 `.env.local`
- 当前 shell 无 `DATABASE_URL`

影响：

- 尚未实际跑过本地或真实环境的 Prisma migration

## 7. 当前外部阻塞项

唯一明确的外部阻塞：

- 未执行 `npx pnpm prisma migrate dev --name auth_persistence_baseline`

原因不是代码失败，而是：

- 没有可用的真实数据库连接

## 8. 已确认的部署决策

已经确认的部署方向：

- **国内云厂商云服务器**
- **Docker 部署**
- **同云厂商 RDS PostgreSQL**

不采用：

- 自建 PostgreSQL 作为最终演示环境
- 海外托管 PostgreSQL 作为最终正式演示方案

## 9. 下一步建议顺序

建议按下面顺序继续推进：

1. 接通真实 Postgres 环境并跑通迁移
2. 补齐 Docker / Nginx / 生产环境变量配置
3. 打通真实的记忆、共鸣、家书主链路服务化
4. 继续拆分 `galaxy-workspace.tsx`
5. 做完整上线 smoke test

## 10. 一句话结论

当前项目已经从“只有前端演示壳”推进到“具备登录、数据库、认证主页、分享主存储、基础验证通过”的**上线地基阶段**。

下一阶段的重心不再是补更多 demo，而是：

- 接通真实数据库环境
- 固化部署
- 把星系主线中的 mock 流程继续替换成真实业务流程
